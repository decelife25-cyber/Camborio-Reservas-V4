import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!;
const SUPABASE_SERVICE_ROLE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
const GMAIL_CLIENT_ID = Deno.env.get('GMAIL_CLIENT_ID') || '';
const GMAIL_CLIENT_SECRET = Deno.env.get('GMAIL_CLIENT_SECRET') || '';
const GMAIL_REFRESH_TOKEN = Deno.env.get('GMAIL_REFRESH_TOKEN') || '';
const GMAIL_FROM = (Deno.env.get('GMAIL_FROM') || 'Camborio.reserva@gmail.com').trim();

const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false, autoRefreshToken: false } });
const cors = {'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json; charset=utf-8'}});
const clean=(v:unknown)=>String(v??'').trim();
const phone=(v:unknown)=>clean(v).replace(/\D/g,'');
const code=(v:unknown)=>clean(v).toUpperCase();
const validEmail=(v:string)=>/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v);
function formatDate(v:unknown){const s=clean(v);if(/^\d{4}-\d{2}-\d{2}$/.test(s)){const [y,m,d]=s.split('-');return `${d}-${m}-${y}`;}return s||'-';}
function formatTime(v:unknown){const s=clean(v);const m=s.match(/^(?:.*T)?(\d{1,2}):(\d{2})/);return m?`${m[1].padStart(2,'0')}:${m[2]}`:s;}
function esc(v:unknown){return String(v===null||v===undefined?'':v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#039;');}
function statusText(v:unknown){const s=clean(v).toUpperCase();const map:Record<string,string>={PENDIENTE:'Pendiente de confirmación',CONFIRMADA:'Confirmada',SENTADA:'Cliente sentado',CANCELADA_CLIENTE:'Cancelada por el cliente',CANCELADA_LOCAL:'Cancelada por el restaurante',FINALIZADA:'Finalizada',NO_PRESENTADO:'No presentado'};return map[s]||s||'Pendiente de confirmación';}
function detailRow(label:string,value:unknown){return '<tr><td>'+esc(label)+':</td><td>'+esc(value||'-')+'</td></tr>';}
function stateRow(state:unknown){return '<tr><td>Estado:</td><td>'+esc(state)+'</td></tr>';}
function notesRow(value:unknown){return '<tr><td>Observaciones:</td><td>'+esc(value||'-')+'</td></tr>';}
function buildHtml(r:any,publicUrl:string){const estadoAmigable=statusText(r.Estado),fechaVisible=formatDate(r.FechaReserva),horaVisible=formatTime(r.HoraReserva),codigoReserva=clean(r.CodigoReserva),telefonoReserva=clean(r.Telefono)||'-',emailReserva=clean(r.Email)||'-',observacionesReserva=clean(r.Observaciones)||'Sin observaciones';let urlConsulta='';const base=clean(publicUrl);if(base){const sep=base.includes('?')?'&':'?';urlConsulta=base+sep+'p=publico&telefono='+encodeURIComponent(telefonoReserva)+'&codigo='+encodeURIComponent(codigoReserva);}const textos:any={PENDIENTE:'Tu reserva está pendiente de confirmación. Te avisaremos cuando el restaurante la confirme.',CONFIRMADA:'Tu reserva está confirmada. Si necesitas cambiar algo, accede al panel público o llama al restaurante.',SENTADA:'Esta reserva ya figura como cliente sentado.',CANCELADA_CLIENTE:'Esta reserva aparece cancelada por el cliente.',CANCELADA_LOCAL:'Esta reserva aparece cancelada por el restaurante.',FINALIZADA:'Esta reserva aparece finalizada.',NO_PRESENTADO:'Esta reserva aparece marcada como no presentado.'};const textoEstado=r.MensajeEstadoPrivado||textos[clean(r.Estado).toUpperCase()]||'Puedes consultar el estado actual de tu reserva en el panel público.';const fechaCreacionVisible=r.FechaCreacion?new Intl.DateTimeFormat('es-ES',{timeZone:'Europe/Madrid',day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(r.FechaCreacion)).replace(',',' a las'):'';const link=urlConsulta?'<p style="margin:7px 0;text-align:center"><a href="'+esc(urlConsulta)+'">ACCEDER A MI RESERVA</a></p>':'';return '<div style="margin:0;padding:4px;background:#f5efe6;font-family:Arial,Helvetica,sans-serif;color:#2b2118;font-size:11px;line-height:1.2"><div style="max-width:440px;margin:0 auto;background:#fff;border:1px solid #e3d5c4;border-radius:9px;overflow:hidden"><div style="text-align:center;padding:7px 8px;border-bottom:2px solid #f2a100"><div style="font-family:Georgia,serif;font-size:16px;font-weight:700;color:#5b260f">TABERNA CAMBORIO</div><div style="font-family:Georgia,serif;font-size:10px;font-weight:700;color:#0d5a22">Cervecería · Tapería</div></div><div style="padding:8px"><h1 style="margin:0;color:#7a241d;font-size:15px">Reserva recibida</h1><p style="font-size:10.5px">Hemos recibido tu solicitud de reserva.</p><div style="background:#fff4df;border:1px solid #e8c47d;border-radius:7px;padding:6px;text-align:center"><div style="font-size:7px">Código de reserva</div><div style="font-size:20px;font-weight:700;color:#7a241d">'+esc(codigoReserva)+'</div></div><table style="width:100%;border-collapse:collapse;font-size:10.5px">'+detailRow('Nombre',r.Nombre)+detailRow('Teléfono',telefonoReserva)+detailRow('Email',emailReserva)+detailRow('Fecha',fechaVisible)+detailRow('Hora',horaVisible)+detailRow('Personas',r.Personas)+stateRow(estadoAmigable)+notesRow(observacionesReserva)+'</table>'+(fechaCreacionVisible?'<p style="text-align:center;font-size:9.5px">Reserva creada el '+esc(fechaCreacionVisible)+'</p>':'')+'<p style="font-size:10.5px">'+esc(textoEstado)+'</p><p style="font-size:10.5px">Puedes consultar, modificar o cancelar tu reserva desde el panel público indicando:<br><strong>Teléfono:</strong> '+esc(telefonoReserva)+'<br><strong>Código de reserva:</strong> '+esc(codigoReserva||'-')+'</p>'+link+'<div style="margin-top:7px;padding-top:6px;border-top:1px solid #eadfd2;text-align:center;font-size:10.5px"><p>Calle Real, 184 · 11100 San Fernando</p><p>Tel. 956 25 45 32</p></div></div></div></div>';}
function utf8Base64Url(value:string){const bytes=new TextEncoder().encode(value);let binary='';for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(binary).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/g,'');}
function encodeHeader(value:string){return `=?UTF-8?B?${utf8Base64Url(value)}?=`;}
async function gmailAccessToken(){if(!GMAIL_CLIENT_ID||!GMAIL_CLIENT_SECRET||!GMAIL_REFRESH_TOKEN)throw new Error('Falta configurar Gmail en Supabase.');const body=new URLSearchParams({client_id:GMAIL_CLIENT_ID,client_secret:GMAIL_CLIENT_SECRET,refresh_token:GMAIL_REFRESH_TOKEN,grant_type:'refresh_token'});const response=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const data=await response.json();if(!response.ok||!data.access_token){console.error('Gmail OAuth error',response.status,data?.error,data?.error_description);throw new Error('No se pudo autorizar el envío con Gmail.');}return data.access_token as string;}
async function sendGmail(to:string,subject:string,html:string){const token=await gmailAccessToken();const mime=[`From: Taberna Camborio <${GMAIL_FROM}>`,`To: ${to}`,`Reply-To: ${GMAIL_FROM}`,`Subject: ${encodeHeader(subject)}`,'MIME-Version: 1.0','Content-Type: text/html; charset=UTF-8','Content-Transfer-Encoding: 8bit','',html].join('\r\n');const response=await fetch('https://gmail.googleapis.com/gmail/v1/users/me/messages/send',{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({raw:utf8Base64Url(mime)})});const data=await response.json();if(!response.ok||!data?.id){console.error('Gmail send error',response.status,data?.error?.message||data);throw new Error('Gmail no ha podido enviar el correo.');}return data.id as string;}
async function findReservation(telefono:unknown,codigo:unknown){const p=phone(telefono),c=code(codigo);if(!/^\d{9}$/.test(p)||!c)return null;const {data,error}=await supabase.from('Reservas').select('ReservaID,CodigoReserva,FechaCreacion,FechaReserva,HoraReserva,Nombre,Telefono,Email,Personas,Observaciones,Estado,FechaEstado,Mesa,Zona,MesasAdicionales,Turno,FechaModificacion,OrigenReserva').eq('Telefono',p).eq('CodigoReserva',c).maybeSingle();if(error)throw error;return data;}
async function registerNotificationPending(reservaID:string,dest:string){const {data,error}=await supabase.from('Notificaciones').insert({FechaHora:new Date().toISOString(),ReservaID:reservaID,Tipo:'EMAIL_CONFIRMACION',Destino:dest,Estado:'PENDIENTE'}).select('*').maybeSingle();if(error){console.error('No se pudo registrar Notificaciones PENDIENTE',error.message);return null;}return data;}
async function updateNotificationStatus(ref:any,reservaID:string,dest:string,estado:'ENVIADA'|'ERROR'){let query=supabase.from('Notificaciones').update({Estado:estado});if(ref?.NotificacionID)query=query.eq('NotificacionID',ref.NotificacionID);else query=query.eq('ReservaID',reservaID).eq('Destino',dest).eq('Tipo','EMAIL_CONFIRMACION').eq('Estado','PENDIENTE');const {error}=await query;if(error)console.error('No se pudo actualizar Notificaciones a '+estado,error.message);}
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({ok:false,error:'Método no permitido.'},405);try{const body=await req.json();const r=await findReservation(body.telefono,body.codigo);if(!r)throw new Error('Reserva no encontrada. Comprueba teléfono y código.');let dest=clean(body.email)||clean(r.Email);if(!dest||!validEmail(dest))throw new Error('Introduce un email válido.');if(dest!==clean(r.Email)){const {error}=await supabase.from('Reservas').update({Email:dest,FechaModificacion:new Date().toISOString()}).eq('ReservaID',r.ReservaID);if(error)throw error;await supabase.from('Clientes').update({EmailUltimo:dest}).eq('Telefono',phone(r.Telefono));r.Email=dest;}const subject=`Confirmación de reserva ${clean(r.CodigoReserva)} - Taberna Camborio`;const html=buildHtml(r,clean(body.publicUrl));const notif=await registerNotificationPending(r.ReservaID,dest);try{const messageId=await sendGmail(dest,subject,html);if(notif)await updateNotificationStatus(notif,r.ReservaID,dest,'ENVIADA');await supabase.from('Log').insert({FechaHora:new Date().toISOString(),Usuario:'PUBLICO',Accion:'ENVIAR_EMAIL',ReservaID:r.ReservaID,Detalle:`Email enviado por Gmail a ${dest}`});return json({ok:true,sent:true,messageId});}catch(sendError){if(notif)await updateNotificationStatus(notif,r.ReservaID,dest,'ERROR');console.error('Email send failed',sendError);return json({ok:false,error:'No se pudo enviar el correo en este momento.'},400);}}catch(e){console.error(e);const err=e as any;return json({ok:false,error:err?.message||'Error interno.'},400);}});
