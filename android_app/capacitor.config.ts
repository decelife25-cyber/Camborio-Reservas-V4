import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.camborio.reservas.privada',
  appName: 'Camborio Reservas Privada',
  webDir: 'dist'
};

export default config;
